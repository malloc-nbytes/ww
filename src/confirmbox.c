#include "confirmbox.h"
#include "glconf.h"

int
confirmbox(const char *msg,
           const char *extra_option)
{
        (void)msg;
        (void)extra_option;
        //gotoxy(unsigned x, unsigned y);
        return 0;
}
