#+TITLE: TODO
#+AUTHOR: malloc-nbytes

* TODO
- fix: handle two open buffers of the same file basename
- fix: handle tabs correctly and non-1-byte characters
- fix: program segfaults when invoking =M-x show-cfg-vars= for the second time due to how clear_output_buffer works
- fix: redraw buffer automatically after window resize
- fix: insert buffers from =initial-buffers= cfg variable in reverse order
- fix: adding italic color to =selection-highlight= cfg var affects =menu-highlight= cfg var
- add: write the default config (=src/default-config.c=) instead of empty file when creating the config for the first time
- add: highlight whole buffer
- add: manually make buffer read-only
- add: previous buffer the first option in switching buffers context
- add: list all global symbols in source code files
- add: absolute line numbers
- add: relative line numbers
- add: auto-indent
- add: variables to ww-calc
- add: floats to ww-calc
- add: bitwise to ww-calc
- add: parenthesis to ww-calc
